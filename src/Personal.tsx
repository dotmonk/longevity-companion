import React, { Component } from 'react';
import { MainProps } from './Main';

export default (props: MainProps) => {
    return (
        <div>
            FIXME: Personal data
        </div >
    );
}
